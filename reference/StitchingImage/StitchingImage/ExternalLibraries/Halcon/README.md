# HALCON runtime libraries

Place the HALCON .NET runtime DLLs in this folder so the application can load them at build/runtime:

- `halcondotnetxl.dll`
- `hdevenginedotnetxl.dll`

These files are provided by MVTec HALCON and are not stored in this repository.
